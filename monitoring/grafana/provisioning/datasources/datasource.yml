apiVersion: 1

deleteDatasources:
  - name: Prometheus
    type: prometheus

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # имя сервиса из compose
    isDefault: true
    jsonData:
      timeInterval: 1s
